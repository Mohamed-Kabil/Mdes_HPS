mc_divergence/.env
notifications.db
__pycache__/
*.pyc

# Local dev logs (front/server.py runs), regenerated on every launch
front/*.log

# Claude Code local permission overrides -- machine-specific, not shared config
.claude/settings.local.json
